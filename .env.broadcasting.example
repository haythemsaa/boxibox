# ============================================
# CONFIGURATION BROADCASTING WEBSOCKETS
# ============================================
# Choisir entre 3 options :
# 1. Pusher (service cloud payant mais simple)
# 2. Soketi (alternative gratuite, self-hosted)
# 3. Log (pour développement sans WebSockets)

# ============================================
# OPTION 1 : PUSHER (Service Cloud)
# ============================================
# Créer un compte sur https://pusher.com
# Créer une app et copier les credentials

BROADCAST_DRIVER=pusher

PUSHER_APP_ID=your_app_id
PUSHER_APP_KEY=your_app_key
PUSHER_APP_SECRET=your_app_secret
PUSHER_APP_CLUSTER=eu
PUSHER_HOST=
PUSHER_PORT=443
PUSHER_SCHEME=https

# Variables pour Vite (frontend)
VITE_PUSHER_APP_KEY="${PUSHER_APP_KEY}"
VITE_PUSHER_APP_CLUSTER="${PUSHER_APP_CLUSTER}"
VITE_PUSHER_HOST=
VITE_PUSHER_PORT=443
VITE_PUSHER_SCHEME=https

# ============================================
# OPTION 2 : SOKETI (Self-Hosted Gratuit)
# ============================================
# Installation Soketi :
# npm install -g @soketi/soketi
# soketi start
#
# Ou avec Docker :
# docker run -p 6001:6001 quay.io/soketi/soketi:1.4-16-debian

# BROADCAST_DRIVER=pusher

# PUSHER_APP_ID=app-id
# PUSHER_APP_KEY=app-key
# PUSHER_APP_SECRET=app-secret
# PUSHER_APP_CLUSTER=mt1
# PUSHER_HOST=127.0.0.1
# PUSHER_PORT=6001
# PUSHER_SCHEME=http

# VITE_PUSHER_APP_KEY="${PUSHER_APP_KEY}"
# VITE_PUSHER_APP_CLUSTER="${PUSHER_APP_CLUSTER}"
# VITE_PUSHER_HOST=127.0.0.1
# VITE_PUSHER_PORT=6001
# VITE_PUSHER_SCHEME=http

# ============================================
# OPTION 3 : LOG (Développement sans WebSockets)
# ============================================
# BROADCAST_DRIVER=log
# Aucune configuration supplémentaire nécessaire
# Les events seront loggés dans storage/logs/laravel.log
