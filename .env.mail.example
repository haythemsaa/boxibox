# =============================================================
# Configuration Email - Notifications Boxibox
# =============================================================

# Mode MAILER
# Options: smtp, sendmail, mailgun, ses, postmark, log
MAIL_MAILER=log

# Configuration SMTP (Gmail, Outlook, etc.)
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=votre-email@gmail.com
MAIL_PASSWORD=votre-mot-de-passe-application
MAIL_ENCRYPTION=tls

# Expéditeur par défaut
MAIL_FROM_ADDRESS="noreply@boxibox.com"
MAIL_FROM_NAME="Boxibox"

# Email admin pour notifications
# Utilisé pour recevoir les notifications de paiements, nouveaux clients, etc.
MAIL_ADMIN_EMAIL="admin@boxibox.com"

# =============================================================
# Configuration Gmail (si vous utilisez Gmail)
# =============================================================
# 1. Aller sur https://myaccount.google.com/security
# 2. Activer la validation en 2 étapes
# 3. Générer un "Mot de passe d'application"
# 4. Utiliser ce mot de passe dans MAIL_PASSWORD
#
# MAIL_HOST=smtp.gmail.com
# MAIL_PORT=587
# MAIL_USERNAME=votre-email@gmail.com
# MAIL_PASSWORD=xxxx xxxx xxxx xxxx (mot de passe d'application)
# MAIL_ENCRYPTION=tls
# =============================================================

# =============================================================
# Configuration Mailgun (service externe)
# =============================================================
# MAIL_MAILER=mailgun
# MAILGUN_DOMAIN=mg.votredomaine.com
# MAILGUN_SECRET=key-votre-cle-api
# MAILGUN_ENDPOINT=api.mailgun.net
# =============================================================

# =============================================================
# Configuration Postmark (service externe)
# =============================================================
# MAIL_MAILER=postmark
# POSTMARK_TOKEN=votre-token
# =============================================================

# =============================================================
# Mode LOG (développement - emails enregistrés dans logs)
# =============================================================
# Aucun email réel n'est envoyé
# Les emails sont écrits dans storage/logs/laravel.log
# Parfait pour développement local !
#
# MAIL_MAILER=log
# =============================================================

# =============================================================
# Tests emails
# =============================================================
# Pour tester l'envoi d'emails sans impact, utilisez :
# php artisan tinker
# Mail::raw('Test', function($msg) { $msg->to('test@example.com')->subject('Test'); });
# =============================================================
